>更新内容记录在此处，每次更新release时作为releasenote发布，发布后清理已发布内容。

## 重要更新

- **打开终端界面进新会话还是最近会话，可以自己选了**：`miyu config` → 全局参数设置的最后一项「打开终端界面时进入」（网页设置里在「终端集成会话默认模式」下面），默认「新会话」，和以前一样；改成「最近会话」后，敲 `miyu` 直接回到这种模式上次用的那条会话接着聊。大厅里按 Tab 换模式后发的第一句照旧开新会话。
- **设置界面里编辑已有项的表单不再有「保存」「返回」两行**（`miyu config`）：改完按 Esc 退出，改过的先收下，最后和别的改动一起由主菜单的「保存并退出」写盘，选「不保存」就整批丢掉；什么都没改就按 Esc，等于没进来过（不会顺手把当前供应商切过去）。添加自定义模型、新建人格、新增任务这类新增的表单照旧有这两行。表单底下那行「导航中」也去掉了，打字时照旧显示「编辑中」。编辑人格、用户身份这几张暂时照旧。
- **footer 看得出缓存断了几次**（终端）：Σ 后面的括号里，断过缓存就多一个「断N」，比如 `(C89%·断1)`；`/usage` 底下列出最近每一次是什么时候、重算了多少、为什么（系统提示词变了、工具表变了、历史里哪一条被改写，还是请求一字未改、供应商没接住）。判法照 Claude Code：一次请求本来能从缓存读到的，实际读到的不到 95%、而且少读了 2000 词元以上，就算断一次；压缩之后的下一次、daemon 重启后的第一次不算。子代理的也算进主会话。C% 的口径不变。**需要重启 daemon**。
- **回合跑着时 `/compact` 排队了**：以前 AI 正在跑（尤其是挂着子代理一等十几分钟的那种），`/compact` 只会报「Miyu is busy」。现在像插话一样排上：这一批工具跑完就把这一轮之前的历史压掉、接着往下跑；这一轮没走到那一步就结束的，结束后马上补压。终端排队区挂一行「/compact 排队中」，网页上的压缩也一样排队。**需要重启 daemon**。
- **后台任务做完的那一行点得开了**（终端全屏）：铃铛那一行（子代理做完、后台命令跑完的汇报）点开，看得到它交回来的东西：子代理的结论（照正文排版）或失败原因，命令是输出的最后几行。刚跑完、回合中途插进来、重开终端回放时都一样。非全屏（inline、shell 集成）还是原来那一行。
- **MCP 服务器按会话常驻，并读服务器说明**：同一会话里连续调用 MCP 工具复用同一个服务器进程，浏览器、数据库这类有状态的服务器能记住上一步（以前每调一次就新起一个进程）；闲置 10 分钟、清空或删除会话、改配置时自动收掉。服务器握手时给的使用说明会带进系统提示词。**行为变化**：MCP 服务器进程现在关进调用它的会话的沙盒（和命令、脚本一样），工作目录是这一轮的工作区。要写沙盒外面（比如浏览器缓存）的服务器，在设置页的服务器对话框里加「额外可写目录」，或者把「沙盒」改成「不关」（只对你自己的会话生效，成员会话永远关着）。**需要重启 daemon**。

- **终端里正文边流边出**：还没写完的那一行、还没闭合的代码块，流着的时候就露在底下，写完了照常落进正文。以前要等一整行（代码块要等整块）写完才出现，长代码块流着的时候正文区像卡住一样。代码块露出来的样子和最终一样（带高亮和边框，只露最后几行，行数跟「思考滚动窗」是同一个设置），mermaid 图和公式先露源码、闭合后再出图。全屏、非全屏、shell hook、单次 `miyu "…"`、后台回写都一样；管道和 `--plain` 的输出不变。**需要换新的 miyu 二进制**；后台回写那条路要重启 daemon。

- **方向键能进任务条、能在命令候选里挑了**（终端）：
  - 输入框里打 `/` 出来的命令候选，用 ↑↓ 挑一条，Tab 补全，回车直接执行（要参数的命令只补全，等你接着打）。候选多了面板跟着滚。没用方向键挑过时，回车还是只认打全的命令名。
  - 光标在输入框最后一行时按 ↓，进到底下的任务条：↑↓ 在上面挪，回车就是点它（子代理切进它的会话，后台命令打开日志）。切过去以后光标还在任务条上，停在刚切过去的那一条，可以接着切。在最上面一条再按 ↑、按 Esc 或者直接打字，都回到输入框。翻上键历史的时候 ↓ 还是翻历史。
  - 任务条最多露 5 条，多了底下写「↓ 还有 x 个」，挪下去跟着滚。

- **子代理的会话能切进去看、接着聊了**（终端全屏和网页）：
  - 点时间线上子代理那一行、或者输入框底下任务条上它那一行，切进它自己的会话：它在跑的话从头看它做了什么，跑完了看得到它的完整对话。以前点开的是盖在正文上的浮层。
  - 网页里点子代理那张卡片的抬头，同样打开它的会话；子会话里输入框上方有一块「↑ 主会话 · 名字」，点它回去。网页切进一个正跑着的子会话时，之前做过的步骤要等这一轮跑完或刷新才出来。
  - 子会话里第一句画成「来自主会话的任务」，点开看全文；任务条最上面钉着「○ 主会话」，点它回去，也可以敲 `/back` 一层层退；状态行上写着「子代理 ↳1」。进出子会话一下就换好，不再一连闪好几个画面，也不再弹「已切换到会话」。
  - `/subagent` 列出这条会话派出过的子代理（连同它们的子代理、各自的状态），挑一个切进去。`/session` 里照旧只有主会话。
  - 任务条改成一棵树：子代理是空心的 ○，正在看的那条是实心的 ●，后台命令照旧转圈。第一层只列这条会话自己派的子代理和命令，它们名下还在跑的收成一个数，比如「开发中（+3）」；有子代理在跑时，最上面一行是「● 主会话」。切进子代理以后还是同一棵树：主会话钉在最上面，一路切进来的每一层都展开，下面用 ├ └ 挂着它的子代理和命令，兄弟也列着，点哪条就切到哪条。
  - 子代理那一行：标题后面跟着它此刻在做什么（放不下就不写），右边是它烧了多少词元、跑了多久，前台、后台的都有。后台子代理不再一件事列两行（子代理一行、后台任务一行）。后台命令照旧，点开是日志面板。
  - 回合跑着的时候切走不打断它，它在后台接着跑；回来时从这一轮开头接着看。在子会话里按 Esc 只是不看了，不会停掉它；要停就按 Ctrl+C（网页上点停止），连它名下的孙代理和命令一起停，停下的子代理也不会再把上一层叫醒。
  - 主会话里按 Ctrl+C 停后台任务时，连子代理带孙代理整棵树一起停，包括你切进子代理会话里接着聊时它又派出去的那些。
  - 在子会话里说的话直接发给那个子代理。
  - 父会话里子代理那一行（网页上是那张卡片）只显示它在干什么、烧了多少词元、跑了多久，过程都在它自己的会话里看。以前终端点开是盖在正文上的浮层、网页是卡片里展开的过程，这两样去掉了；网页任务条上后台子代理那一行也改成点进它的会话。09-18 之前的老会话，网页上子代理卡片照旧能展开看当时记下的过程。非全屏（inline、shellhook）下「显示工具调用信息 = 完整」也不再把子代理内层的每一步印出来，只留那一行。
  - **需要重启 daemon**。

- **长会话往上翻得到开头**：
  - 全屏终端里切回一条会话、重开 TUI 时，先画最近一屏；往上翻到顶，会接着从库里补更早的一页，一直翻得到第一句。以前固定只回放最近 3 轮，更早的翻不到。
  - 非全屏（inline、shell 集成）只印最近一屏。
  - 「重开 TUI 回放的轮数」这个设置删掉了。配置文件里留着这一项也不碍事，回退到上一版照样认。
  - 网页打开会话只取最近 30 轮，往上滚到顶再补更早的；回合跑着的时候，每秒的同步也只取最近一页。以前每次都把整个会话拉下来，最重的会话一次约 3 MB。
  - **需要重启 daemon**（网页前端也跟着换，重启后刷新一次网页）。

- **会话数据收进数据库，终端开得更快**：
  - 终端启动、换会话时不再一遍遍体检整个会话库。以前终端启动要把库打开 6 次，每次都把整个文件检查一遍；现在一个进程只开一次，检查和清理只在 daemon 里做（实测 40 MB 的库，开 6 次从 119 ms 降到 7 ms）。
  - 上键历史不再为了几句原话把整段对话读出来。
  - 待办、上键历史、会话钉住的思考档位这些按会话存的东西，从 state 目录下的零散文件搬进了会话库，删会话时一起删掉。以前删会话会留下这些文件，还有超长输出的落盘和压缩转录，一直没人清。
  - 用量账本搬进了 `state/usage.db`。以前每调一次模型，都要把 usage.json 读出来、改完再落盘（实测每次 2–4 ms）；现在记一笔几乎不花时间，统计页的数字不变。
  - 升级后第一次用到时，老文件会自动导进来。这一版老文件先留着，回退到上一版照样能用。
  - **需要重启 daemon**。

- **同一轮里的多个只读工具一起跑**：AI 一次发出好几个读文件、搜文件名和内容、搜网页、抓网页的调用时，现在同时跑，全部跑完后按调用顺序交回，给模型的顺序和内容都不变；多个子代理本来就是同时跑的，现在也走同一套。执行命令、改文件、发消息、提问这类有副作用的照旧一个一个来。**需要重启 daemon**。
- **开发模式默认不再带提示词**：以前开发模式会先塞一句内置的「You are a helpful software engineer assistant.」，现在默认为空，系统提示词只有主机环境（系统、工作目录、模型这些事实）。想要就在 `miyu config` 的开发模式里自己写，写什么发什么；老版本自动写进 `dev-prompt.md` 的那句会被当成没写，你自己改过的内容照旧生效。新装机不再预先生成 `dev-prompt.md`。**需要重启 daemon**。
- **过程收起后的那一行说得更具体了，回复末尾多了一行 ✻**：
  - 收起那一行以前统一是「Worked for 12s · 7 tools · 2 thoughts」，现在按做了什么分开写，也不再挂耗时：「运行了 3 次命令 · 编辑了 2 次 · 用了 4 个工具 · 思考了 1 次 · 出错了 1 次」（动作在前、思考在后、出错垫底；英文界面是「Ran 3 commands · 2 edits · 4 tools · 1 thought · 1 err」），只想了想写成「思考了 5.2 秒」。编辑只数改文件的那几步，知识库、artifact 和其余工具都算工具。终端和网页一个写法；设置里那一项改名为「过程收起成一行摘要」。
  - 一轮说完，回复下面多一行 `✻ deepseek-v4.1-flash · 处理了 3 分 14 秒 · 1:53 完成`：谁答的、这一轮用了多久、几点结束。动词在几个词里挑，同一轮不管重画、回放还是在网页上看都是同一个；被打断的轮写「中断」（回放里取代原来那行「已中断」；被打断的轮现在也记下是哪个模型答的）；不是今天的带日期。开着「显示本次供应商 / 模型」时，模型那里写成「供应商 / 模型」，不再单独占一行。全屏和常驻的终端、网页都有，单次 `miyu "…"` 和 shell 集成里没有；网页上被打断的轮照旧是「本轮已中断」那一行。
  - **需要重启 daemon**。
- **QQ 长回复转成的图好看多了**：
  - 文里的 `<br>` 画成换行，以前会原样印在表格格子里；`<b>`、`<i>`、`<s>`、`<code>` 转成对应的粗体、斜体、删除线和代码样式，其余标签只留字。
  - 换栏不再留大片空白：放不下的表格和段落先拆进这一栏剩下的地方，再续到下一栏（表格续栏重复表头）；小标题不再孤零零挂在栏底，跟它下面的内容待在同一栏。
  - 长宽跟着内容量走：短回复一栏往下长，长了再分栏，整张图尽量接近方形。以前长文的高度卡在 2600，只能一栏栏往横里加，成了又宽又扁的长条，太长时还会整张渲不出来。
  - 图里的 mermaid 图：字改用和正文同一套字体（以前会被系统里的等宽字体顶上，中文一个字一个字隔开，终端里出的图也一并修了）；长图里图上的字放大到正文的三分之二左右，思维导图改成树状排法、收进一栏，底下也不再是一块白板。
  - 设置里的「长图最大高度」去掉了，配置里留着的旧值不再起作用。**需要重启 daemon**。
- **技能改成插件**：技能从「子系统」挪到了「插件」，和脚本、MCP 同一类。用起来没有任何变化，模型看到的内容也一个字节没变。`miyu config` 功能表里「技能」那一行从「子系统」一节挪到「插件」一节；`persona.toml` 里技能的开关改写在 `[plugins] enabled` 名单里（`"skills"`），老文件里的 `[subsystems] skills = …` 读进来时自动折算，技能开没开跟升级前一样，下次保存时换成新写法。**需要重启 daemon**。
- **QQ 群聊可以单独设限流了**：「私聊/群聊专属配置」里会话类型选群聊时，多了一项「覆盖群聊限流」，可以给某个群单独定「多少秒内最多回几轮」，不管它在不在白名单；不勾选就还按白名单 / 非白名单群聊的限流走。网页设置和 `miyu config` 都能改。**需要重启 daemon**。
- **QQ 里管控命令的回执 3 秒后自己撤回**：`/reset`、`/reset-memory`、`/reset-all-memory`、`/stop` 回的那句（「当前会话已重置。」「已打断 1 个运行中的任务。」之类，失败提示和用法提示也算）发出 3 秒后自动撤掉，群里不再留着机器人的内部状态，她下一轮看到的群聊记录里也没有这条。`/wipe`、`/models` 的回执照旧留着，你发的那条命令本身不撤。**需要重启 daemon**。
- **QQ 群里主动插话收敛了一些**：
  - 她自己凑过去接话的那两种（随机抽中、或者刚说完话后留意到的下一句），判断通过后不再先在对方消息上贴「在看了」的表情，直接回。被 @、正跟这个人聊着的照旧贴。
  - 刚说完话那段时间（观察窗口）的加分从 0.15 降到 0.10。你自己改过这一项的，以你的设置为准。
  - 冷静机制重做：以前是「每回一次冷静度 +1、每 3 分钟回落 1」，热闹的群一下午能攒到上百，于是一整天都被压着，忙完要十几个小时才缓过来。现在只看她最近说了多少话：每回一轮记一笔，3 分钟衰减一半，停下来十来分钟就恢复。说一句几乎不影响，连着说到第三、四句门槛明显抬高，说得再多也最多抬 0.35。
  - 冷静只管插嘴：别人 @ 她、跟她说话、说她、接她刚说的话，她照常回，不会因为刚说了很多就装没看见；只有往别人的对话或开放话题里插嘴时，才按最近说了多少来收着。
  - 冷静机制不再分轻/中/强档，设置里只剩「回复克制」一个开关；原来的恢复时间、强度、倍率三项去掉了，配置里留着的旧值会被忽略。
  - 短消息（6 个字以内）不再额外抬高回复门槛：判断模型自己就会给这类消息低分，这一项是重复计算。
  - 判断模型不再看到程序的加减分数字，只按消息内容打分，加减分由程序事后算，不再重复计算。
  - **需要重启 daemon**。
- **daemon 重启不再打断正在做的事**：换了新版本重启 daemon、daemon 崩溃或被杀时，正在跑的那一轮以前会停在「已中断」，要你重新说一遍。现在新 daemon 起来后会自己接着做：会话里多一行「↻ 重启了，接着上一轮继续」，她会看到上一轮做到哪儿了、哪条命令是被打断的，从断的地方往下做。终端、网页、QQ 都一样。
  - 她开出去的子代理也一起接着跑：挂回原来那条会话名下，跑完结果照常送回来；她那一轮要是也被打断了，续跑时会被告知哪几个子代理还在跑，不会再派一遍。
  - `/goal` 的续轮被打断了也接着跑，自动续轮随后照常往下走，不用再 `/goal resume`。
  - QQ 里的那一轮等账号重新连上再接。
  - 不限次数、不限时间：连着被打断几次就接几次（提示里写「第 n 次」），隔多久再启动都接。之后你已经接着说话了的那一轮不再接。
  - 被打断时正在跑的那条命令，系统不会替她重跑一遍（它可能已经生效了），她会先查一下再决定要不要重来。
  - 你按停止（Ctrl+C、Esc、网页上的停止）的那一轮，停了就是停了，重启后也不会自己跑起来。
  - **需要重启 daemon**（从这次重启之后才生效）。
- **思考档位是每个会话自己的了**：终端的 `/effort` 和网页模型菜单里的档位小片，改的都只是当前这条会话，别的会话不受影响；别的会话正在跑的时候也能改，这条会话下一轮起用新档位。菜单第一项「跟随全局（…）」回到全局默认档；选「default / 模型默认」是这条会话固定用模型默认档（全局设了档位也不带）。全局默认档在 `miyu config` 或 `miyu effort` 里改。网页上钉了档位的小片字是强调色。**需要重启 daemon**。
- **输入框旁边能看到这一轮用了多久**：终端底部状态行里声波右边、网页输入框下面那一排最右边，多了这一轮对话的总用时（`12s` / `1m 05s` / `1h 02m 05s`），跑着的时候一秒一跳，跑完就收起来。终端里状态行放得下的时候，每秒 token、上下文和累计 Σ 照旧跟在状态行右端；窗口窄到放不下时，才挪到输入框下面原来空着的最底下那一行。网页那部分**需要重启 daemon**。
- **网页可以一次删除多个会话**：在某条会话的「…」菜单里点「多选…」，侧栏会进入选择模式，勾上要删的几条，点顶上那条操作栏的「删除」。正在看的那条也被删了的话，会切到剩下的某一条。**需要重启 daemon**。
- **`/clear` 现在和 `/reset` 一样**：清空当前会话，终端和网页都认。原来的 `/clear` 只是清屏，要清屏请按 Ctrl+L。
- **会话之间的 AI 可以互相发消息了**（新工具「给其他会话发送消息」`send_to_other_running_session`）。终端和网页里（普通、开发模式都有）可以让她给你另一个开着的会话捎话，比如「构建好了告诉写文档的那个会话」。她先列出你名下此刻开着（有终端或网页窗口开着）或正在跑的会话，能看到会话 id、名字、模式、工作目录、会话内容存在哪，再把话发过去：对方正在跑就插进它这一轮，闲着就替它起一轮去回。
  - 收到的一方会看到铃铛加「从 xxx（短 id）收到消息」，发出去的一方时间线上是「给其他会话发送消息 <短 id> <会话名>」。短 id 是会话 id 末尾那 8 位，她也认，你可以直接说「发给 acd86d38」。
  - 正文先露 10 行（暗色，和命令预览一样），点开看全文；行数在设置里改（新增「跨会话AI消息预览行数」）。
  - 对方回不回、回什么由那边的 AI 决定，要回也用同一个工具。只能发给你自己的会话、并且得是开着或正在跑的。
  - 没有常驻窗口的会话收不到回话，比如单次 `miyu "…"` 或终端集成里问的那条。
  - QQ 里没有这个工具，子代理也没有。
  - **需要重启 daemon**。
- **网页会跟着 Miyu 升级自动刷新了**：daemon 换成新版本重启后，已经开着的网页会自己刷新一次，换上新前端。以前要手动刷新，不刷的话旧页面会悄悄缺新功能（比如跨会话消息里，别的会话看不见这个网页开着的会话）。只重启、没换版本时不刷新。这个要从下一次升级起才生效，这次升级后请手动刷新一次网页。

- **只读模式，一个键随开随关**：空会话按 Shift+Tab、非空会话按 Tab 切换全盘只读模式，状态行左下角的模式名会换成金色的「只读」，空会话的模式行上也有一个金色的点亮着（切换时不另弹通知）。开着的时候 AI 能读你电脑上的一切，但哪儿都写不了，连 `/tmp` 也不行：改文件、删文件、命令和脚本写盘都会被拦，她收到的说明是「只读模式开着」，不会误以为换个目录就能写。代价是要临时文件的命令（部分编译、`mktemp`、要存登录态的脚本）在只读下会失败。回合进行中按下去，她下一次动手就生效（claude-code 这类 CLI 供应商要等下一轮）。记忆、待办、知识库、生图和搜图存图这些由 Miyu 自己完成的写入不受影响。沙盒的状态也不再写在系统提示词里，改成变了才补一句，所以切多少次都不会打断模型的缓存。空会话里 Tab 仍然是切 普通/开发。**需要重启 daemon**。
- **默认开启沙盒模式**（设置里新增一项，引导里多了一页专门问）：开着时，你没特意设置过的会话能读全盘，但写只限一个目录——在项目目录里打开 Miyu（或在那里用终端集成问她）就是那个项目；在家目录本身、`~/.miyu` 里、`~/.ssh` 这类隐藏目录里打开，以及网页、语音、QQ 这些没有当前目录的入口，是 `~/.miyu/home/<你>/workspace`。防的是 AI 听岔了话、或者被网页和群聊里的内容骗去改你的文件。要换地方就在会话里 `/sandbox <路径>`；在会话内使用 `/sandbox clear` 清除该会话的沙盒。`/sandbox` 查看改成了一张表（根目录、只读、可写、可读），不再夹着英文。生图、搜图这类图片落盘不受影响。新装默认开；**已经在用的机器升级后默认关着**，想要去「全局参数设置 → 默认开启沙盒模式」或网页设置「通用 → 工具」里打开。没有沙盒能力的机器（Linux 内核低于 5.13）开了也不生效，不会让命令跑不起来。

- **macOS（Apple Silicon）可以用 Homebrew 安装了**：`brew install shorin-kiwata/miyu/miyu`，要求 macOS 15 及以上，chafa、ripgrep、onnxruntime 会一起装好。Release 页上的 macOS 包没有签名，用浏览器直接下载会被 Gatekeeper 拦，请用 Homebrew 安装。
- **功能表（OOBE 与 `miyu config`）里名字改清楚了**：语音→语音功能、识图→视觉识别、汇率→汇率查询、Arch Linux→Arch Linux 相关、文件→读写文件、番组日历→番剧信息、游戏兼容性→游戏兼容性调查、在线手册→查询man命令手册。
- **功能表支持 `Ctrl+A`**：一次全开、再按一次全关（有没勾的就先全勾）。`miyu config` 里是新加的，OOBE 里本来就有、只是按键条没写出来。
- **「启用的功能」不再摆这四件**：读写文件、外发、人格提醒、情绪与好感度——开关照旧生效，只是不占表上的位置（想摆回来删一行即可）。
- **「查 DeepSeek 状态」模型现在直接看得到**：它以前藏在技能后面，那份技能删掉后就没人想得起它；现在它是常驻工具。
- **两份重复的技能删掉了**：`Linux 游戏兼容性`（能力由 `game_compat` 脚本承担）与 `抖音/TikTok 下载`（下载脚本照旧可用）——同一个能力不再同时以「技能 + 脚本」两份出现在模型面前。

- **WebUI 有英文界面了**。以前网页里的话全是写死的中文，英文用户只能硬看。现在网页语言跟「设置 → 界面 → 界面语言」走：选「自动」时看浏览器语言（中文浏览器中文、其余英文），选「英语」或「简体中文」时整个 Miyu 都用那一种（命令行、终端界面、网页、桌面通知、语音面板一并跟随）。数字与日期也跟着换格式（千分位、月名、12/24 小时制）。在设置页改完保存，网页会自己重载、当场生效；服务端发下来的那部分文字（看板标题、输入框提示、报错、语音会话名、子代理进度）同样按浏览器语言给。**需要重启 daemon**（网页资源与语言判断都在主程序里）。
- **技能改成按人格放进资源目录、从磁盘读取**：内置技能不再编进程序，住在 `share/miyu/personas/<人格>/skills/<技能名>/`，技能自带的脚本也跟着住进去。功能表里「技能 + 它的脚本」只剩一行（比如「哔哩哔哩直播控制」原来会出现两次），这一行就是两者共同的开关。老位置的脚本照旧能被找到，升级不会丢东西。出厂脚本也一起搬进了同一棵树（`share/miyu/personas/default/scripts/`），`miyu paths` 会多显示一行「系统人格资源目录」。

## 修复

- 让 AI 列出你开着的其他会话时，时间线上那一步直接写「列出其他会话」，不再写成「给其他会话发送消息 · 列出开着的会话」，看着像在给谁发消息。终端和网页一样。**需要重启 daemon**。
- **在 `/session` 里删会话，不会再删着删着面板自己关掉、接着把别的会话误删**（终端）：以前删一条正在跑、或者挂着后台子代理的会话，会先把它的回合停掉、子代理拆掉，最后一步却因为子代理交回的汇报又起了一轮而删不掉，面板一声不响地关掉；重开面板时光标默认停在当前会话上，再按一下删掉的就是当前会话。现在一开始就把会话占住（新的一轮起不来），删不掉就什么都不碰，原因写在面板里，面板不关。正在跑的会话（它自己或名下的子代理在跑）要按两下 Ctrl+D，第一下只把那一行换成提醒；闲着的照旧一下删掉。删掉自己正待着的那条时，先落到另一条会话上，面板在原位开回来。**需要重启 daemon**。
- 开着 QQ 的机器上，daemon 重启或 QQ 断线重连之后，所有会话的缓存不会再整段失效了。以前「发送到 QQ」「查 QQ 联系人」这两件工具只在 NapCat 连着的时候才挂上，一连一断就换一份工具表，每条会话的下一轮都要从头重算；现在只要配置里开着 QQ，它们就一直在，没连上时调用会照实说没连上。**需要重启 daemon**。
- 改一个上千行的文件，时间线上那一步的 `+N -M` 和点开的 diff 不会再算成整份删了重写（比如只改了一行，却显示 `+1269 -1285`）。以前两边行数一乘超过 25 万就不再逐行比，五百行以下的文件一直是对的，更大的文件改一处就露馅；现在只数真正改了的行，文件再大也一样快。模型那边不受影响。**需要重启 daemon**。
- 全屏终端里换会话（`/session` 里挑一条、进出子代理会话）一次就换好：以前每切一次先后闪出六到八个中间画面，中间还有一帧空屏。回到一条正在跑的会话时，footer 上的上下文和累计 Σ 也不会再先掉成一个偏小的数（比如 99k 掉成 25k）、等下一次请求才纠正：现在用 daemon 那边的实时数。**需要重启 daemon**。
- **压缩不再把一段闲聊当成摘要存下，也不再丢掉命令输出**：压缩上下文时，模型要是没照摘要的格式写、顺着对话聊了下去，以前这段话会被当成摘要存下，折叠掉的历史就只剩一句闲聊；现在先让它照格式重写一次，还不行就换一条路重新总结，再不行才退回占位（手动 `/compact` 会直接报错，什么都不折）。另外，摘要请求没法复用缓存的几种情况下（关了缓存复用、上下文超长后的兜底压缩等），摘要器以前只看得到对话文字、看不到跑过的命令和工具输出，开发会话压一次就把命令和报错全忘了；现在一并带上（输出截到 2000 字，参数只留名字）。**需要重启 daemon**。
- **一轮结束时的几样记录一次写完**：回复、工具步骤、上下文用量、输出速度现在同一笔写进会话库。以前分几笔写，中途出错或进程被杀，会留下「显示已完成、最后几步工具却没记上」的轮。**需要重启 daemon**。
- daemon 给终端、网页分发事件时不再给每个订阅者各拷一份、再整条解析一遍（实测同一串事件快 3.6 倍、少拷 16 MB），别的会话在刷屏时占用更低。**需要重启 daemon**。
- MCP 服务器的几处问题：回合被打断或超时时服务器进程不再泄漏；npx、uvx、浏览器这类服务器起的子进程随服务器一起收掉，不再留成孤儿；改完 MCP 配置后第一次对话不再让别的会话一起卡住、等它列工具；服务器出错时报错里带上它 stderr 的最后几行。**需要重启 daemon**。
- 写技能时，回执和 skill-creator 技能让 AI 去调两个早就不存在的工具（apply_patch、publish_skill），现在改成了现有的 edit 和 manage_skill 发布。**需要重启 daemon**。
- **大厅和选择面板在不支持同步输出的终端上不再闪**：空会话的大厅每 40 毫秒动一拍，以前每拍都把输入框和打开着的面板（`/session`、`/effort`、`/models`）先擦掉再写一遍，终端要是不支持同步输出就看得见它们一闪一闪；现在只写变了的星星，输入框和面板内容没变就不动。AI 回复的时候输入框也不再每来一段就重写一遍。kitty 这类支持同步输出的终端上看不出区别。
- **切进正在跑的子代理会话、或者回合中回到主会话时，做过的步骤不再都显示「<1ms」**（终端全屏）：以前补出来的那几步耗时一律是 <1ms，子代理那一步的秒数也会从头数；现在按每一步实际发生的时刻算。**需要重启 daemon**。
- **网页的子代理卡片不再闪出 `__subagent_metric__` 这类字样**：子代理跑着的时候，卡片标题右边那行「在做什么」偶尔会闪出一串带下划线的内部标记；现在不会了，卡片上的词元数也显示出来了（以前一直空着）。子代理没写标题时卡片画不出来的问题也一并修好。**需要重启 daemon**。
- **长回复切走再切回来，不再只剩一半**（终端）：很长的一段回复，切到别的会话或模式再切回来、或者重开终端之后，以前只剩前 2048 个字加一个「…」，最后那几句看不到。现在整段画全。之前留下的会话里，最后交出来的那段回复也会照全文补回来；夹在工具调用中间的长段落补不回来。新的回合要**重启 daemon** 才会整段存下，老会话的补全换新的 miyu 二进制就生效。
- **回合跑着时开面板，正文不再停住**（终端全屏）：AI 正在输出时敲 `/models`、`/session`，或者她反问弹出提问面板，以前正文停在开面板那一刻，面板收掉才接着往下画。现在面板开在输入框的位置上，上面的正文照常往下流；PageUp/PageDown 翻上面的正文。面板里挑完、答完照旧生效；回合在面板开着时跑完了，选择面板接着开着让你挑完。另一个终端或网页先答了这道题，这边的提问面板自己收掉。

- **工具列表里「给子代理留言」不再是英文 id**：`miyu tool --list` 和网页的工具清单里，给已开出的子代理追加消息的那件工具以前显示成 `send_subagent_message`，现在和对话里一样叫「给子代理留言」。**需要重启 daemon**。
- **网页退出登录后，光标落在用户名框**：以前退出后光标停在密码框，接着打字会打进密码框；现在用户名空着就先给用户名，和第一次创建管理员那一页一样。**需要重启 daemon**。
- 运行中在设置页新加的模型，要重启 daemon 才查得到它的资料：上下文窗口会退回通用的 168000 并标成估计值，思考档位菜单里没有它的档位，记账里没有价格。原因是模型目录只在启动时按当时的配置裁剪一次，之后不再补。现在新加的模型一保存就补进来。**需要重启 daemon**。
- mimo、sensenova 两家的模型在工具调用之间也能接着上一步的思考往下想了：以前这两家不在「回传思考」名单上，每次调用工具之后，模型之前想过的内容就被丢掉。实测它们的网关都收这个字段，也会转给模型，现在已经加进名单。用这两家模型的老会话，第一轮缓存命中会降一次，之后恢复。**需要重启 daemon**。
- 一批工具调用的几处修正：
  - 同一批里有子代理时，它们原来会越过排在前面的其它调用先跑；现在按调用顺序来。失败的子代理原来显示成成功，现在照实显示失败。
  - 模型认图、但供应商不收工具结果里的图时，一批多个调用里带图的那条会把图片消息插在别的工具结果中间，严格的网关直接报错；现在整批结果放完再放图。
  - 有的线路每一片都重发一遍工具名，一个工具调用会被当成好几个，界面上显示成「批量准备」；现在同一个调用只算一次。
  - 提问面板开着的时候，上面不再一直挂着一行「⠋ 准备问题」。
  - **需要重启 daemon**；提问面板那一条只要换新的 miyu 二进制。
- 两种情况下缓存不再整段失效：
  - 工具调用轮数用完的最后一轮（QQ 群聊默认 32 轮）：以前这一轮一个工具都不带，上下文最大的时候前缀从头重算；现在照带同一份工具，只让模型这一轮不调用。个别网关不认这个参数时，自动退回原来的做法。
  - 会话进行中新增、删除或修改技能：以前所有在线会话的下一轮都要整段重算。现在技能目录不再写在工具说明里，改成跟着对话发给 AI：技能有变化，下一轮补发一份新目录，缓存不受影响；新发布的技能 AI 下一轮就看得到，删掉的也不再挂在目录里。升级后每个会话的第一轮会整段重算一次。
  - 直连 Anthropic 官方接口时，请求带上缓存断点（以前完全不缓存）；直连 OpenAI 官方接口时，带上会话级的 prompt_cache_key。只影响直连这两家的用户。
  - **需要重启 daemon**。
- 有的线路（比如 opencodego 的 deepseek、bigmodel 的 glm）会把 AI 写好的整段编辑扣住，写完才一口气放出来。以前 TUI 里这段时间一直显示「思考中」在走秒，「准备编辑」那一行只闪一下、看不见。现在思考停了 1.5 秒没有新动静，就把这段收成「已思考」，秒数停在真正想完的那一刻，下面只留一个转圈，工具名到了再接「准备编辑」。边写边发的线路（官方 DeepSeek 等）不受影响，网页不变。
- AI 改文件、跑命令、抓网页的几处修正：
  - 改过的文件不再变成只有自己能读写（0600），可执行脚本不再因为改一次就丢了执行位；改软链时改的是它指向的文件，软链本身保留（以前会被换成普通文件）。
  - 补丁里删除文件改成移进回收站，删错了能找回来（和 `trash_path` 一样）。
  - 补丁的「匹配到多处」误报少了：只差缩进的另一处不再算；同一份补丁前后两块改的是长得一样的两处时，按先后顺序各改各的。补丁没打上时会说是第几块、从哪一行开始；多个文件写到一半失败时，说清哪些已经改了。
  - 命令跑超时后，已经打出来的输出会交给 AI（以前只剩一句「超时」）；输出太长被截断时，全文存进 `~/.miyu/cache/command-output/` 并把路径告诉 AI，7 天后自动清理。
  - 抓网页碰到 PDF、图片、压缩包这类不是文本的内容时，直接说明是什么类型、建议用命令下载，不再把乱码塞进对话。
  - AI 读文件的结果去掉了一层 JSON 包装，同样的内容少占一成多词元。
  - **需要重启 daemon**。
- 对话长到超出模型的上下文窗口时，会先自动压缩一次再重试，不再直接报错。这个兜底早在 8 月就加了，但一直没生效：压缩时撞上自己这一轮还在「进行中」就放弃了，于是模型一报超长，终端和网页直接显示错误，QQ 里那一轮不回话。**需要重启 daemon**。
- 被限流、额度用完、对话超长时的重试更合理了：
  - 供应商回 429 并说明「几秒后再试」（Retry-After）时，20 秒以内就原地等完再试一次，不再一次节流就整轮失败；池里的端点按它说的时长冷却，不再一律关 10 分钟。
  - 余额或额度用完（402 之类）不再在同一个端点连打，池里有别的端点就直接换过去。以前有的供应商把这类报错写成「请求被拒」，连换端点都不试。
  - 对话超长不再在同一个端点白打三次（每次还要等 2 秒），直接交给自动压缩。
  - **需要重启 daemon**。
- 在 TUI 里回答完 AI 的提问后退出、再打开 TUI 回到那个会话，不会再弹出同一个提问界面了。以前重开的 TUI 会把这一轮从头补画一遍，把早已答过的提问也当成新问题弹出来，并卡在面板上不往下显示；这时按两下 Esc 关掉，还会把正在跑的这一轮取消掉。现在答过的提问直接显示成问答记录，AI 的输出照常往下走；退出时还没回答的提问，重开后照样弹出，可以接着答。同一个会话同时开着两个 TUI（或开着网页）时，在一边答完，另一边的提问面板也会自己关掉并显示同样的问答记录，不用再手动关，也不会误把这一轮取消掉。**需要重启 daemon**。
- 网页会话里，每轮附带的预览文件（artifact）清单没有变化时不再重复附上，长聊天里省下一截上下文；给 AI 的预览文件用法说明里原先点名的三个工具早就不存在了，现在改成了真实可用的那几个。**需要重启 daemon**。
- 打断过的一轮、AI 跑工具时你插进去的话、连着两次一模一样的工具调用、/goal 的收尾提示，之后的下一轮不会再让缓存从那一轮开头整段失效了。以前这几种回合在下一轮里会被重新拼成另一个顺序，和当时发给模型的对不上，上游只能从那一轮的开头重算：大会话里打断一次要白算几千词元、插话一次上万。现在按当时发出去的原样放回，开发模式下 15～21 万上下文实测，每一轮第一条请求的缓存命中从 98.1% 升到 99.8%，被迫重算的词元从 4.4 万降到几百。缓存用量日志（`~/.miyu/cache/logs/cache-usage.*.jsonl`）另外多记了系统提示词和工具表的指纹，工具表中途变了时会记下是哪几件工具。**需要重启 daemon**。
- 网页上 AI 写任务清单的那一步，不会再把一大段内部数据（`__todo_table__{"todos":[…`）当成那一行的说明印出来了，现在写「3 项」这样一句；清单本身照旧显示在下面的任务卡片里。**需要重启 daemon**。
- 网页提问面板的页码在中文界面下不再显示英文「1 of 2」，改成「1 / 2」。**需要重启 daemon**。
- 「本会话用量」这件工具在一轮中途问，不再少算这一轮了。以前它只读已经落账的数，而用量要等一轮跑完才落账，所以头一轮问会得到「还没有落账的用量」，之后每次问都少算正在跑的那一轮；现在和底部的累计 Σ 是同一个数，上下文也是这一轮最近一次请求的。**需要重启 daemon**。
- 手机上用网页时，模型名很长的话，输入框下面那一排右边的上下文数字不再被截掉：模型名改在自己那一格里省略（至少露出一截名字），上下文和这一轮的用时完整显示。**需要重启 daemon**。
- 新开一个终端运行 `miyu`，或者用 `/new` 换到新会话时，状态行上不会再闪出别的会话正在跑的后台任务了。以前终端刚起来那一小会儿会先把所有会话的后台任务都列出来；`/new` 之后，旧会话的任务也要在新会话里挂一秒左右才消失。
- 直连模式（`MIYU_DIRECT=1`）下，AI 回答途中的浮层可以正常操作了：按 Esc 关浮层、按 x 停后台任务、翻页键翻浮层。以前这时按键全进了输入框，连按两下 Esc 想关浮层反倒会打断这一轮；这一轮里新开的后台任务也要等说完才出现在状态行上。
- AI 想得很久（几万词元）时，终端界面不会再越来越卡了。以前思考越长，界面每一帧要重新排的字就越多，想到三万词元左右就把一个 CPU 核吃满，敲一个字要等好几秒才显示；现在不管想多久，界面自己的负担都差不多（实测四万六千词元时约 5% CPU，敲字 10 毫秒上下出来）。
- AI 还在回答的时候，点开底部后台任务的浮层按 x，也能当场停掉那个任务了。以前这时按下去只是把浮层关了，任务要等这一轮说完才真的停。
- 长会话里打断 AI 不再慢半拍了。按 Ctrl+C 或连按两次 Esc，AI 立刻停下、输入框马上能打字；以前会话越长越慢（上下文几十万词元的会话要等好几秒），因为每次打断都要把整段对话从头数一遍用量，而且要数两遍。打断之后 footer 显示的上下文用量也不会再掉到一个偏小的数（比如从 710k 掉到 400k、再聊一句才恢复）：现在按供应商上一轮报回来的实际用量，加上被打断这一轮的内容来算。**需要重启 daemon**。
- 空会话里按 Tab 切换普通/开发模式现在一按就换过去，光标也不会在输入框里左右晃了。切模式只换界面上的显示，等你发第一句话（或者敲命令、按 Shift+Tab 切只读）时才真正换到那种模式的会话上。以前每按一次都要让后台换一次会话，另一种模式上次用的如果是一条很长的会话，还得先把它从头数一遍用量；换的那一小会儿里再按的键会被终端原样显示出来，光标跳到后面再跳回来。切过去那一刻，footer 上那种模式的上下文数就已经在了（启动时后台先算好，不建会话），发第一句时也不会跳。**需要重启 daemon**（没重启时这个数先显示「—」）。
- 空会话里回车发第一句不会再闪一下了。以前回车后先画出一帧「输入框清空了的大厅」，等后台开始这一轮才换成对话的布局；现在回车那一帧直接就是对话布局。
- footer 上的累计（Σ）不会再先掉回旧数、过一两秒再涨回来了。一轮刚结束或者刚打断时，终端界面会拿这一轮开始前读到的旧累计把刚刷新的数盖回去（空会话的第一轮结束时就是 Σ 先消失一下再出现）；空闲时后台子代理跑完、累计已经涨了，一敲命令也会被盖回旧数。现在旧读数一律作废，只认刷新之后读到的。
- 终端界面（全屏 TUI）的时间线不会再错位了。一轮里工具调用一多（整段时间线超过约 250 行），或者 AI 输出途中改了窗口大小，正在跑的那一行（比如「思考中」）会画出好几份、中间隔着大段空白，而且错位会一直留着，只有 `/reset` 才清得掉。现在进行中的那一段每一帧都回到固定的起点重画，不再靠「往上数几行」去找位置。
- 空会话（大厅）里用输入法打字时，光标不再从输入框外面跳回输入框。原来星空动画的每一帧要分成二十多次写给终端，终端（kitty）赶上只读到半帧时，输入法的候选框和光标会先被放到星星那儿、再跳回来；现在每一帧整帧一次写出去。大厅动画的样子不变，写给终端的数据量降到原来的约三分之一（在 herdr 里跑时它要少解析这么多）。
- AI 正在输出时打字，字不再一个一个往外蹦。原来这时候每 16 毫秒才收一个按键，输入法一次上屏六个字要九十毫秒才出全；现在排着的按键一次收完（约 17 毫秒）。没在输出时连续上屏的几个字也只画一帧。
- 一轮跑了几十上百次工具的长回合里，终端界面自己的 CPU 占用明显降低：进行中那段时间线原来每一帧都要把所有已完成的步骤重新排版、重新写一遍，现在只重写变了的那一行。
- `miyu config` 所在的终端被直接关掉（或者 SSH 断开）之后，不会再留下一个吃满一个 CPU 核的残留进程。以前这种情况下它判断不出终端已经没了，会一直空转下去。
- 后台任务跑完的汇报不会再偶尔悄悄丢掉了。任务恰好在会话一轮刚起步的那一瞬间完成时，汇报以前会被直接放弃；任务只报这一次，于是再也看不到。现在等那一轮准备好再插进去。**需要重启 daemon**。
- 回合收尾那一刻才到的后台汇报，不会再被并进已经结束的那一轮、等你下次开口才被顺带看到了：现在会另起一轮去回。**需要重启 daemon**。
- 后台汇报这类系统替你发起的一轮，不会再被当成会话列表里的「最后一句话」，不会被拿去给新会话起名，也不会出现在「重新生成」的候选里。**需要重启 daemon**。
- 全屏终端里补印的后台汇报不会再串到别的会话了。以前按 daemon 的「当前会话」去查，这个窗口开着的不是当前会话时，可能印出别的会话的汇报、漏掉自己的；现在按这个窗口自己的会话查。
- 子代理追话工具的说明改对了：以前还写着改名前的 `task(...)`，也说「只能发给还在跑的子代理」，其实跑完的子代理收到会在后台另起一轮，跑完再叫醒你。**需要重启 daemon**。
- 程序调用 Miyu（`miyu ask`、`miyu stdio`）时给这一轮加的限制，在模型走中转线（claude-code、codebuddy、codex、agy）时终于生效了：`--tools a,b` 只给这几件工具、`--no-tools` 一件都不给、`--no-memory` 这一轮不写记忆。以前中转线一概不认——模型照样看到全部 Miyu 工具，`--no-memory` 的这一轮还摆着「记住」工具，`--no-tools` 时 CLI 自带的 Bash、Edit 也照开。现在只要带了 `--tools` 或 `--no-tools`，CLI 自带的原生工具这一轮一律关掉；带限制的回合另开一条中转会话，不会把工具清单的变化混进原来那条对话。普通模型下 `--tools`、`--no-tools` 以前还会漏出「本会话用量」这一件工具，也一并修了。平时直接聊天不受影响。
- 在 herdr 里用 Miyu，一轮跑完侧栏不会再一直显示「进行中」了。原因是 daemon 由 herdr 某个 pane 里的 `miyu` 拉起时，带上了那个 pane 的身份；它起的 agy / claude / codex（这三家都装着 herdr 的钩子）一启动就把那个 pane 认领成自己的会话，herdr 从此把 Miyu 的上报全部丢掉。现在 daemon 和中转线的 CLI 都不再带 pane 身份。已经卡住的那个 pane 要关掉重开一次才能恢复。
- 在 herdr 里，回合结束和「在等你回答」的弹窗又能看到了（改走系统通知：herdr 会吞掉 kitty 的通知协议）；提示音改由 herdr 自己放（它的 `[ui.sound]`，默认在切到别的 tab 或窗口时响），Miyu 在 herdr 里不再另响一声。
- 对 Miyu 的终端界面发 SIGTERM / 关掉它所在的终端，现在会正常收尾退出，并把 herdr 侧栏那一行释放掉。以前 SIGTERM 要么直接把它杀掉、要么被憋住杀不掉，侧栏上会留着一个已经不在了的 miyu。
- 网页上换会话后，输入框下方的「累计」会换成这个会话的数了。以前删掉正在看的会话、自动切到别的会话（直接在侧栏点别的会话也一样），「累计」一直停在上一个会话的数，要在新会话里跑完一轮才纠正——这个数只升不降，是为了防子代理跑完时数字往下掉，但换会话时没有清零。现在换会话时从这个会话的累计（含子代理的花销）重新算起。**需要重启 daemon**。
- 用时统一写成时分秒。输入框右上角的 `/goal` 提示原来只写秒数（跑了二十多分钟显示成 `1407s`），现在是 `23m 27s`；工具、子代理和后台任务的用时过了一小时原来会把秒丢掉（`2h 03m`），现在都是 `1h 02m 05s` 这样。不到一分钟的照旧（`12s`、`9.9s`）。
- 终端集成在 PATH 之外找到的 Miyu（比如 Homebrew 装在 `/opt/homebrew/bin`，而 shell 没配 `brew shellenv`），现在也找得到同一目录下的 `rg`、`chafa`。以前搜索会报「`rg` 不在 PATH 上，请安装」，其实早就装着。
- `miyu bash-init` 遇到 bash 4 以下（macOS 自带的是 3.2）会直接说明：自然语言交给 Miyu 要 bash 4 以上，请改用 zsh 或 Homebrew 装的新版 bash；直接敲 `miyu` 不受影响。以前装完没有任何提示，敲中文只会报 command not found。
- QQ 长文转图里的链接又有颜色了。她给来源写的是「标题 (地址)」这种纯文本（提示词就是这么要求的），而转图只认 Markdown 链接写法，于是来源和正文里直接贴的地址都跟正文一个颜色。现在图里凡是地址都上链接色（句尾标点不算进去，行内代码里的不算）；标题和括号保持正文色——Markdown 写法的 `[标题](地址)` 也改成同一口径，只有地址是蓝的。另外长图里段落中的单个换行现在照画成换行：以前会被并成一段，「每个来源一行」的几行在图里首尾相接，而同样的话在终端、网页、QQ 短回复里都是分行的。**需要重启 daemon**。
- 知识库设置里不会再说「未配置 Embedding」了。主页明明写着「本地 · bge-small-zh…」，「人格和功能 → 知识库」（以及网页设置 → 插件 → 知识库）却说没配置——那一行读的是早就不用的旧设置项，知识库实际一直在用主页配的那个模型。现在这一行显示的就是主页那个全局 Embedding 模型：在 `miyu config` 里回车直接进主页同一个 Embedding 菜单，网页里点「去设置」跳到「通用 → Embedding」。知识库表单里的「语义最低分」「Embedding 超时秒数」也不摆了：改了从来不生效，真正起作用的是全局 Embedding 高级设置里的同名项。`miyu kb stats` 报的也改成实际在用的模型。网页那部分**需要重启 daemon**。
- 终端集成在 macOS 上能用了。hook 里直接调 `miyu`，PATH 上找不到它时自然语言输入会被悄悄忽略——而 macOS 上 Homebrew 装在 `/opt/homebrew/bin`，没配过 `brew shellenv` 的 shell（fish 常见）找不到。现在 PATH 上没有时 hook 会去 Homebrew、`~/.local/bin`、`~/.cargo/bin` 等常见位置找；哪儿都没有的话，装的时候会提示怎么把它加进 PATH。bash 在 macOS 上改写进 `~/.bash_profile`（macOS 的终端开的是登录 shell，不读 `~/.bashrc`，以前写进去等于没装）；zsh 跟着 `ZDOTDIR` 走。已经装过的 hook 升级后会自动更新；macOS 上用 bash 的需要重跑一次 `miyu bash-init`。
- 受限（沙盒）会话里读得到技能和出厂脚本了。Miyu 装在 `~/.local` 这类非系统前缀下、或从源码树跑时，沙盒只放行了老的脚本目录，技能和搬进新位置的脚本在沙盒会话里都用不了。**需要重启 daemon**。
- 机器上残留着旧版的 `/usr/share/miyu/scripts` 时，不会再把脚本和技能读错位置——会优先选带新布局的那一份。
- 搜图不再做下载后的「视觉模型审核」。审核用的模型一出错（比如免费模型被限流），整批图都会被判成不安全，报出来的是 `rejected by safety review`，真正的原因被藏住了。设置里的「视觉模型审核」开关一并删除；开着安全搜索时，不再使用本身没有安全过滤的百度和 360 两个图源。**需要重启 daemon**。
- 在一轮对话里改了生图的设置（比如让她帮你把 API key 写进配置），这一轮接着生图就会用上新设置，不用等下一轮。**需要重启 daemon**。
- 终端里（非全屏）偶尔有图显示不出来：到回合结束还没打出来的图现在会补打，不再悄悄丢掉。设环境变量 `MIYU_IMAGE_TRACE=1` 可以把图片显示的每一步记进 `~/.miyu/cache/logs/image-trace.log`，方便追查。
- 关掉某个技能后，它自带的脚本不会再被 AI 调用。以前关掉「哔哩哔哩直播控制」，AI 照样能直接调开播脚本；现在技能一关，它的脚本一并不可用，整个技能功能关掉时也一样。**需要重启 daemon**。
- 开发模式（`/dev`）里现在也能问她「这个会话用了多少 token」了。以前这件工具跟着全局用量查询一起开关，而 dev 的清单两件都不挂——写代码时想看上下文占了窗口多少、这条会话累计烧了多少，她手上一个字都没有。现在「本会话用量」只认一条判据：取得到这条会话的账就装上；**全局那件仍然不进 dev**（写代码时全局账不影响任何决策）。需要重启 daemon。
- macOS 上编不过的一处修好了。`trim_process_memory` 这个函数在 Linux 和非 Linux 两支上可见性写得不一致，跨 crate 引用时在 macOS 编译直接失败——Linux 永远编得过，所以一直没人发现。同时给 CI 加了 macOS 这一条流水线（编译、语音那一面、单元测试），以后再往里带 Linux 专属假设，提交当场就会被拦下。
- macOS 上也不会再留下没人收尸的后台进程了。由启动者带起来的后台进程，在启动者被强杀之后会跟着退出——这件事 Linux 上早就做了，macOS 缺一个对应的内核机制，一直空着。
- 少装了东西时，报错会说清缺的是什么。以前在没有 ripgrep 的机器上，让她搜代码只会得到一句「No such file or directory (os error 2)」——连缺的是哪个程序都没说，她只能去猜路径、换参数，白白浪费几轮。现在直接说是哪个程序不在 PATH 上，以及怎么装。
- Arch 专属的那几件工具（AUR 查询/审查安装、Arch 新闻、ArchWiki）不再默认出现在非 Arch 系统上。以前无论 macOS 还是 Debian，这些工具都会挂在她的工具清单里白占额度，其中 AUR 那几件在那些系统上必然失败。现在默认跟着系统走；**想要的话在「人格和功能」里勾上照样能用**。已经在用的非 Arch 机器会自动关掉一次，需要的自己开回来。引导的功能屏里也不再摆这一行，设置界面保存时只会打开你这次亲手勾上的项，不会再把它们顺手打开。
- macOS 上那几个要浏览器的脚本（小红书、知乎、闲鱼检索）现在认得出系统里装的 Chrome 了。以前只在 PATH 上找，而 macOS 的浏览器装在应用包里、不在 PATH 上，于是明明装着 Chrome 也会被判成「没有可用浏览器」。
- 打断一轮之后，会话累计的 token 不会再「凭空少一截」了。以前按下打断，那一轮已经花掉的 token 会整个从账上消失——footer 的 Σ 直接掉回打断前的数字。实测一个被打断的轮真实烧了 18.7 万 prompt、命中 12.3 万缓存，库里记的却是 0。现在打断的轮照实记账。
- 对话会话不会再「裁剪」掉最早的对话了。上下文满了一律交给压缩（把旧对话折成摘要留在上下文里），裁剪那条路在对话里彻底不跑。裁剪是**直接删**最老的几轮——删掉的内容只进归档库、不再回到对话，而且历史开头一变，模型那边的缓存前缀就要从头重算一次。平台（群聊）那边配置本来就是「不压缩、直接丢」，保持原样。
- 模型报错时，被拒的请求会留下一行「形状」记录（各类消息各几条、有没有配不上对的工具调用）。以前遇到中转吞掉字段路径的报错（比如只说一句「应该是个字符串」），而失败的那一轮又会整个回滚、库里一行不剩，事后完全无从查起。
- macOS 上的会话沙盒能用了。`/sandbox` 和成员的受限会话原来在 macOS 上直接被拒（底层走的 Landlock 是 Linux 独有的），现在改走系统自带的 `sandbox-exec`。**有一处和 Linux 不一样要知道**：macOS 这条路只锁得住「写」，锁不住「读」——环境说明里会照实写成「可读：全部」，不会假装锁上了。
- macOS 上问电池会有答案了。「电池养护」这个脚本原来只认 Linux 的电池接口，在 macOS 上一律回「这台机器没有电池」；现在会去读系统的电源信息，台式机（比如 Mac Studio）照旧照实说没有电池。改设置那一半 macOS 上做不了，会明说做不了而不是假装改成功。
- 英文界面下不再中英混杂。功能开关列表、内置插件、技能名这三处原来只有中文，把界面切成英文之后它们仍然是中文，夹在英文里很突兀。现在这三处都有英文名，技能名也会从 id 拼成人话（`travel-planner` → `Travel planner`）而不是直接显示 id。
- 在 opencode 的端点上用视觉分析工具不会再整轮失败了。工具把图片读回来之后，图是直接放进「工具结果」里发给模型的——但 OpenAI 兼容的接口规定工具结果只能是纯文本，opencode 那边照规范严格校验，于是整个请求被拒（HTTP 422），那一轮白跑，端点还会被冷却到 8 分钟。现在这类端点会自动改走另一条路：工具结果只放文字，图片紧跟一条独立消息发出去。**图还是原图、还是交给当前模型自己看**，不会退回去用你配的那个多模态模型转述。官方 DeepSeek 一类本来就接受的端点不受影响。
- Claude Code、Codex、CodeBuddy、Antigravity 这几个 CLI 后端不在 PATH 上也找得到了：PATH 里没有时，Miyu 会再去 Homebrew（`/opt/homebrew/bin`）、`~/.local/bin`、`~/.cargo/bin` 等常见安装位置找。以前在 macOS 上，Claude Code 的官方安装脚本把它装在 `~/.local/bin`，Miyu 的 daemon 继承到的 PATH 里却常常没有这个目录，于是报「Claude Code CLI not found」，引导里的「模型通道」也说没找到 CLI。设置里填了程序路径的，仍以设置为准。**需要重启 daemon**。
- 小红书扫码登录时，二维码不会再一直换了。等你扫码的时候，脚本每秒都去确认一次登录了没有，而确认要重新打开页面，页面一开站点就换一张新码——人根本来不及扫（实测 90 秒换了 22 张）。现在只在登录真正完成时才去确认。
- macOS 上能用小红书检索了：以前一运行就报错（读了 Linux 才有的 `/proc`）。搜索和看笔记在后台用无头浏览器，不弹窗、不抢焦点；只有扫码登录时弹出浏览器窗口给你扫。搜索被小红书风控拦住的话，可以设 `XHS_HEADED=1` 改成开窗口的方式（会弹窗）。
- macOS 上知乎检索能登录了。以前 `zhihu-search login` 在 Mac 上一律报「登录需要图形界面」，一旦被知乎的验证码拦住，就没办法手动过。
- macOS 上小红书、知乎、闲鱼、酒店比价这几个要用浏览器的脚本，不再关掉 Chrome 自带的安全沙箱（关沙箱只是给 Linux 容器里跑用的）。
- Linux 上中断小红书登录（比如按 Ctrl+C）不会再留下一个 Xvfb 进程。
- 用 claude-code、codex 这类 CLI 当模型时，她拿到的工具和平时对上了。这几条线的工具是另拼的一份清单，漏了两道规则：开着语音唤醒时，每个会话都挂着一件只有语音对话才用得上的「结束语音对话」工具；子代理在这几条线上拿得到本不该给它的工具（闹钟、表情包、技能、向你提问等），孙代理还能接着开子代理。现在两边用同一套规则。**需要重启 daemon**。
